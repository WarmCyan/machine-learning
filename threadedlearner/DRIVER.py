import learner
import learner2
import utilities

from curses import wrapper

def main(stdscr):
    pass
    #stdscr.clear()
    #util = utilities.Utilities(stdscr)

    #learner2.initialize(util)

    #util.print("Hello there!")
    #util.print("Yup, yup, yup", 1)
    #util.wait()
    #util.wait()

if __name__ == "__main__":
    #wrapper(main)
    #l = learner.Learner(True)
    #util = utilities.Utilities()
    learner2.initialize()
